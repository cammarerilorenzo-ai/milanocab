

## Rinominare "In attesa" in "In attesa di conferma"

Modifica puramente testuale nel componente `RideRequestCard.tsx`, dove lo stato `pending` viene visualizzato.

### Dettagli tecnici

**File: `src/components/RideRequestCard.tsx`**
- Trovare la stringa `"In attesa"` associata allo stato `pending` e sostituirla con `"In attesa di conferma"`.

